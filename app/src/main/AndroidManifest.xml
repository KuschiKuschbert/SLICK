<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Location (staggered: FINE+COARSE at onboarding, BACKGROUND when convoy created) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service types (Android 14+ requires explicit foregroundServiceType) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Bluetooth P2P mesh + SCO audio (Cardo/Sena helmet) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- IMU crash detection + convoy state -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- SCO audio routing for TTS alerts to helmet -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Signal recovery beacon (dead-zone detection) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- SOS + convoy notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Battery optimization bypass prompt -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Wake lock for sensor polling in SURVIVAL_MODE -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Camera for convoy QR code scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera for QR code scanning in Convoy Lobby (Join tab) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Hardware features -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.wifi" android:required="true" />

    <application
        android:name=".app.SlickApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Slick"
        android:hardwareAccelerated="true"
        android:largeHeap="false">

        <!-- Disable Sentry's ContentProvider auto-init so we can control DSN from SlickApplication.
             Without this, SentryInitProvider runs before Application.onCreate() and crashes
             when no DSN meta-data tag is present in the manifest. -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <!-- Main entry activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Engine Room: Persistent foreground service -->
        <!-- foregroundServiceType="location" mandatory for Android 14+ background location -->
        <service
            android:name=".service.ConvoyForegroundService"
            android:foregroundServiceType="location"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- WorkManager job for PMTiles delta updates -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- BroadcastReceiver for signal recovery (dead-zone beacon) -->
        <receiver
            android:name=".engine.signal.SignalRecoveryReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SERVICE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Activity Recognition transitions: IN_VEHICLE/STILL for auto Pre to In-Flight -->
        <receiver
            android:name=".engine.location.ActivityTransitionReceiver"
            android:exported="false" />

    </application>
</manifest>
