# Default (non-secret) values for the Secrets Gradle Plugin.
# These are checked into version control.
# Override sensitive values in local.properties (gitignored).

OPEN_METEO_BASE_URL=https://api.open-meteo.com
OPEN_METEO_API_KEY=""
VALHALLA_BASE_URL=https://valhalla1.openstreetmap.de
P2P_SERVICE_ID=com.slick.tactical.mesh.v1
SQLCIPHER_PASSPHRASE_ALIAS=slick_db_key_v1

# Sentry crash reporting DSN (leave empty to disable -- safe default for dev)
# Create an Android project at https://kuschmierz.sentry.io and paste the DSN here.
SENTRY_DSN=""

# These must be set in local.properties -- no defaults:
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# PMTILES_URL=
